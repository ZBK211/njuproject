# 提交清单

## 三项提交物

1. 公开 Git 仓库：提交完整项目代码。创建仓库后，把公开 URL 填到 `README.txt` 第一行。
2. `README.txt`：控制在 1000 字以内，不写姓名、学校、API Key 等身份信息。
3. MP4 演示视频：2 分钟以内、200MB 以内。视频由本人录制，最终与 `README.txt` 打包提交。

## 录屏建议

录制前先打开一个干净终端，进入项目目录：

```powershell
cd C:\Users\MR\Desktop\南软实训
python -m pytest -q
python scripts/run_demo.py
python scripts/demo_server.py
```

视频节奏：

0:00-0:15 展示项目目录和 `README.txt`。

0:15-0:40 展示 `coding_agent/agent.py`、`coding_agent/tools/registry.py` 和 `coding_agent/tools/shell.py`，说明核心循环、工具注册和安全执行是自己实现的。

0:40-1:30 运行 `python scripts/run_demo.py`，保留 `list_dir`、`read_file`、`write_file`、`run_command` 和 pytest 通过结果。

1:30-1:50 打开 `http://127.0.0.1:8787`，点击“运行一次”，展示网页中的 transcript、生成代码、pytest 输出、项目记忆和合规检查。

1:50-2:00 总结没有使用被禁止的 Agent 框架，也没有使用服务端代码执行或文件工具。

## 打包检查

只检查 `README.txt`：

```powershell
python scripts/prepare_submission.py
```

录好视频后生成 zip：

```powershell
python scripts/prepare_submission.py --video path\to\demo.mp4 --name 你的姓名
```

打包脚本只放入 `README.txt` 和 MP4，不会把源码、缓存或密钥放进提交 zip。
