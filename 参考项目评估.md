# dsh-memoir 参考评估

参考仓库：`https://github.com/Qinling-Melon-Farmers/dsh-memoir.git`

本地位置：[tmp/dsh-memoir](C:/Users/MR/Desktop/南软实训/tmp/dsh-memoir)

## 结论

`dsh-memoir` 可以参考和复用设计思想，许可证是 Apache-2.0，工程质量也比较高；但它不能直接作为本次考核项目提交。原因是它的定位是 DeepSeek Harness 的本地项目记忆插件，而题目要求的是独立设计并实现一个 coding agent。插件本身不包含完整的 coding agent 主循环、模型动作解析、工具执行循环和终止策略。

更合适的做法是：保留我们的 ForgeAgent 作为主体，把 `dsh-memoir` 里成熟的“项目记忆层”能力迁移进来。当前已经完成 Python 版迁移：本地结构化记忆、可读投影、记忆检索、Hot Memory 有界注入、自动收尾记录。

## 能复用的部分

1. 项目记忆模型：把工作记录、经验教训、行动指南和备注分区保存。
2. 本地持久化：数据只落到本机项目目录，不依赖云端记忆服务。
3. Hot Memory：下一次 agent 启动时，只注入预算内的高价值记忆，而不是把历史全塞进 prompt。
4. 检索思路：中文短语、英文关键词、代码标识符分词，再用 BM25 排序召回。
5. 重复治理：写入前识别近似重复，避免记忆文件越用越乱。
6. 可观测演示：生成 `PROJECT_MEMORY.md`，让评审能直接看到 agent 做完任务后沉淀了什么。

## 不能直接用的部分

1. DSH 插件宿主：它依赖 `@deepseek-ai/dsh-*` 运行环境，本项目要求可独立运行。
2. Web GUI：很强，但会拉高前端工程量，且不是题目核心评分点。
3. 自动 turn-stopping 钩子：这是 DSH 平台事件机制，我们的 agent 需要自己在 `Agent.run()` 中实现收尾。
4. TypeScript 构建链：我们的项目选用 Python 标准库，降低依赖和解释成本。

## 已经迁移到 ForgeAgent 的提升

- [coding_agent/memory.py](C:/Users/MR/Desktop/南软实训/coding_agent/memory.py)：本地结构化记忆、BM25 召回、Hot Memory 预算控制、重复检测。
- [coding_agent/tools/memory.py](C:/Users/MR/Desktop/南软实训/coding_agent/tools/memory.py)：`memory_record` 和 `memory_read` 工具。
- [coding_agent/agent.py](C:/Users/MR/Desktop/南软实训/coding_agent/agent.py)：系统提示词自动注入项目热记忆，任务完成后自动记录工作摘要。
- [scripts/run_demo.py](C:/Users/MR/Desktop/南软实训/scripts/run_demo.py)：离线演示会展示代码修改、测试通过和生成的项目记忆。
- [tests/test_memory.py](C:/Users/MR/Desktop/南软实训/tests/test_memory.py)：覆盖中文/代码分词、BM25 排序、重复检测和 Hot Memory 预算。

## 和题目要求的对应关系

符合点：

- Agent 主循环由 ForgeAgent 自己实现。
- 工具定义、注册、执行和错误返回都在本项目中。
- 文件工具和命令执行都是本地执行，没有使用服务端代码执行工具。
- 没有使用 LangChain、LlamaIndex、OpenAI Agents SDK、AutoGen、CrewAI 等 Agent 框架。
- 支持上下文压缩、动作解析失败恢复、命令安全拦截、步数上限终止。

需要注意：

- `tmp/dsh-memoir` 只是参考仓库，不进入最终公开仓库提交。
- 如果后续直接复制 Apache-2.0 源码片段，需要保留许可证和明显修改说明。
- 最终视频要展示 ForgeAgent 自己的 agent loop，而不是只展示记忆模块。
