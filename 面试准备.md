# 面试准备

## 60 秒中文介绍

我实现的是 ForgeAgent，一个面向本地代码仓库的编程智能体。它的核心不是套用 Agent 框架，而是自己实现了四部分：第一，用消息列表保存任务、模型动作和工具观察结果，并在上下文超限时保留系统提示、任务和最近轨迹；第二，用明确的 JSON 协议让模型一次选择一个工具或返回最终答案；第三，用工具注册表把文件读写、搜索和命令执行统一分发到本地，并限制文件路径在工作区内、限制命令时间和输出大小；第四，在每轮解析动作、执行工具、反馈结果，并用解析失败、模型错误和最大步数保证循环可终止。项目同时提供不联网的确定性 FakeModel 和 pytest 测试，因此演示可以稳定复现。真实模型只通过环境变量接入，密钥不会进入仓库。

## English introduction (under one minute)

Hello, I built ForgeAgent, a lightweight coding agent for local workspaces. Instead of using an existing agent framework, I implemented the core loop myself. The agent keeps a message history, asks an OpenAI-compatible language model for one JSON action at a time, executes local file or shell tools, and feeds the observation back to the model. The file tools are restricted to the workspace, shell commands have timeouts and bounded output, and the loop has explicit recovery and termination rules. I also added a deterministic offline model and automated tests, so the main workflow can be reproduced without an API key. My design goal was to make the agent small enough to explain line by line, but robust enough to complete and verify a real programming task.

## 2 分钟视频脚本

0:00-0:15：展示目录和题目工作区，说明这是一个本地代码任务，目标是实现 FizzBuzz 并通过测试。

0:15-0:35：展示 `Agent.run`、`ToolRegistry` 和系统提示，说明模型每次只能返回一个 JSON 动作，工具结果会再次进入消息历史。

0:35-1:25：运行 `python scripts/run_demo.py`。画面中保留 list_dir、read_file、write_file、run_command 和 pytest 通过结果。不要展示任何 API Key、姓名、学校或个人路径。

1:25-1:50：展示测试文件，说明测试覆盖路径穿越、精确编辑、命令返回码、解析失败恢复和最大步数终止。

1:50-2:00：总结：这是自己实现的工具执行、输出解析、上下文管理、错误处理和循环终止，而不是现成 Agent SDK 的封装。

## 高频问题与答法

### 1. 为什么不用 LangChain 或 Agents SDK？

题目明确要求核心机制自行实现。更重要的是，手写后可以清楚解释消息如何变化、工具如何执行、错误如何回传以及循环何时结束。模型客户端仍然可以使用兼容 API，但 Agent 状态机在本项目中。

### 2. 模型输出不是合法 JSON 怎么办？

解析器支持普通 JSON 和 json 代码围栏；失败后把明确错误作为观察结果反馈给模型，请它重新输出。连续三次失败直接结束，避免无限重试。

### 3. 如何避免读写工作区之外的文件？

所有相对路径都与工作区根目录合并并 resolve，然后检查结果是否仍是根目录或其子路径。越界请求只返回错误，不执行文件操作。

### 4. 为什么工具结果放回 user 消息？

当前使用兼容性最好的纯文本协议，工具结果作为结构化 JSON 放回对话，模型能看到调用工具和观察结果。迁移到原生 tool calling 时只需替换适配层，执行器本身不变。

### 5. 如何防止 Agent 无限运行？

每轮只执行一个动作，并设置最大步数。模型错误、连续解析错误和 API 错误也都会返回明确状态。命令自身还有超时和输出上限。

### 6. 上下文太长怎么办？

按字符预算检测；保留系统提示、原始任务和最近若干轮，把早期轨迹替换成一条上下文压缩标记。生产版本可以再加入模型生成的摘要和 token 级计数。

### 7. 这个设计的主要局限是什么？

当前工具是单进程本地执行，尚未提供审批 UI、增量补丁、token 精确计费和并发工具调用。它们是有意识的后续迭代方向：先保证核心状态机可解释、可测试、可复现，再扩大能力。

### 8. 如何证明不是“看起来能跑”？

离线 FakeModel 固定输出动作序列，测试会实际创建文件、读取文件、执行 pytest，并检查最终文件内容；另外测试错误 JSON、越界路径、编辑次数不匹配、命令退出码和步数上限。

### 9. 是否参考了 dsh-memoir？能不能直接用？

我参考了它的项目记忆设计，它的 Apache-2.0 许可证允许复用，但它本身是 DSH 插件，不是独立 coding agent，不能直接替代本题的主体实现。我迁移的是适合本项目的能力：本地结构化记忆、可读投影、中文/英文/代码标识符分词、BM25 检索、Hot Memory 有界注入和重复记录治理。ForgeAgent 的 agent loop、动作解析、工具执行、错误恢复和终止策略仍然是本项目自己实现的。

### 10. 项目记忆有什么实际价值？

它解决的是跨会话遗忘问题。Agent 完成一次真实工具调用后，会把“用了哪些工具、最后结果是什么”自动写入 `.agent/memory.json`，并生成 `.agent/PROJECT_MEMORY.md`。下一次运行时只注入预算内的高价值记忆，不会把历史全量塞进上下文；需要长尾信息时可以调用 `memory_read` 检索。
